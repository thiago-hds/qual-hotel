<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= hotel ? 'Editar': 'Novo' %> Hotel</title>
</head>

<body>
    <form action="/hotels/<%= hotel? `${hotel._id}?_method=PUT` : '' %> " method="POST">
        <div>
            <label for="name">Nome</label>
            <input type="text" id="name" name="hotel[name]" value='<%= hotel?.name %>'>
        </div>
        <div>
            <label for="location">Local</label>
            <input type="text" id="location" name="hotel[location]" value='<%= hotel?.location %>'>
        </div>
        <button>Salvar</button>

    </form>
    <a href="/hotels">Voltar</a>

</body>

</html>