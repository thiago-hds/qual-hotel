const factory = require('../../libs/factories');
const HotelService = require('../hotel_service');

describe('Hotel Service', () => {
  describe('findAll()', () => {
    // it('should find all hotels', async () => {
    //   // Arrange
    //   const numberOfHotels = Math.floor(Math.random() * 10);
    //   await factory.createMany('Hotel', numberOfHotels);
    //   // Act
    //   const hotelService = new HotelService();
    //   const hotels = hotelService.findAll();
    //   // Assert
    //   expect(hotels.length).toBe(numberOfHotels);
    // });
  });
});
